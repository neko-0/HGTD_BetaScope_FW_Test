name: Docker Image CI

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    container: rootproject/root
    steps:
      - name: pull repo
        uses: actions/checkout@v2
      - name: ls
        run: ls
        
